{{ form_start(form) }}

<div class="mt-3 mb-3">
	{{ form_row(form.prenom) }}
</div>
<div class="mb-3">
	{{ form_row(form.nom) }}
</div>
<div class="mb-3">
	{{ form_row(form.email) }}
</div>

<div class="mb-3">
	{{ form_row(form.telephone) }}
</div>
<div class="mb-3">
	{{ form_row(form.message) }}
</div>
<button class="bouton mt-3">{{ button_label|default('Envoyer') }}</button>

{{ form_end(form) }}
