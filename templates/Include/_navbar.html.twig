<nav class="navbar navbar-expand-lg bg-body-tertiary">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{'accueil'}}"><img class="rounded w-50" src="{{ asset('build/images/logo.png') }}" alt="Logo"></a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarNav">
			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link active" aria-current="page" href="{{ path('app_index') }}" title="Page accueil">Accueil</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_propos') }}" title="Page propos">A propos</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_contact') }}" title="Page contact">Contact</a>
				</li>
				{% if app.user %}
					<li class="nav-item">
						<a class="navlink" href="{{ path('app_admin_index') }}" title="Page administrateur">administrateur</a>
					</li>
					<li>
						<a class="bouton" title="se déconnecter" href="{{ path('app_logout') }}" data-no-swup="">
							Se déconnecter
						</a>
					</li>
				{% else %}
					<li>
						<a class="bouton" title="se connecter" href="{{ path('app_login') }}" data-no-swup="">
							Se connecter
						</a>
					</li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
